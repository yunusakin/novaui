name: novaui

services:
  frontend:
    container_name: novaui-prod
    image: novaui-prod:latest
    build:
      context: ../..
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    env_file:
      - ./.env.prod
    environment:
      - NODE_ENV=production
    restart: unless-stopped
