services:
  frontend:
    container_name: novaui-test
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    env_file:
      - ./.env.test
    environment:
      - NODE_ENV=test
    restart: unless-stopped
